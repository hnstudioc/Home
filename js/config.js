/* ============================================================
   88Phone 홈페이지 설정 파일
   ------------------------------------------------------------
   앱을 구글 플레이에 등록한 뒤, 아래 값만 고치면
   사이트 전체(다운로드 버튼 · QR 코드 · 후기 링크)가 한 번에 바뀝니다.
   다른 파일은 손댈 필요가 없습니다.
   ============================================================ */

window.SITE_CONFIG = {

  /* ---- 1. 출시 상태 ------------------------------------------------
     플레이스토어 등록이 끝나면 false → true 로 바꾸세요.
     false 인 동안에는 사이트에 "출시 준비 중" 안내가 표시되고
     다운로드 버튼이 비활성화됩니다.                                  */
  RELEASED: false,

  /* ---- 2. 구글 플레이 주소 -----------------------------------------
     등록 후 실제 주소로 교체하세요.
     PACKAGE_ID 만 바꾸면 아래 세 주소가 자동으로 맞춰집니다.          */
  PACKAGE_ID: 'com.hanullstudio.phone88',

  /* ---- 3. 문의 연락처 ---------------------------------------------- */
  CONTACT_EMAIL: 'HNStudio.c@gmail.com',

  /* ---- 4. 후기·개선요청 접수 방법 ----------------------------------
     구글 폼(또는 네이버 폼)을 만들었다면 그 주소를 넣으세요.
     비워 두면(''), 사이트 안에서 내용을 작성한 뒤
     메일 앱으로 보내는 방식으로 동작합니다.                           */
  REVIEW_FORM_URL: '',
  FEEDBACK_FORM_URL: '',

  /* ---- 5. 사이트 주소 (QR 코드에 사용) ------------------------------ */
  SITE_URL: 'https://www.hanullstudio.com/'
};

/* ---- 위 값에서 자동으로 만들어지는 주소들 (수정 불필요) ---- */
(function (cfg) {
  cfg.PLAY_STORE_URL =
    'https://play.google.com/store/apps/details?id=' + cfg.PACKAGE_ID;
  /* 스마트폰에서 열면 플레이스토어 앱이 바로 실행되는 주소 */
  cfg.PLAY_STORE_APP_URL = 'market://details?id=' + cfg.PACKAGE_ID;
  /* 플레이스토어의 별점·리뷰 작성 화면 */
  cfg.PLAY_STORE_REVIEW_URL = cfg.PLAY_STORE_URL + '&showAllReviews=true';
})(window.SITE_CONFIG);
