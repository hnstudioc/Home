/* ============================================================
   88폰 홈페이지 설정 파일
   ------------------------------------------------------------
   구글 플레이 등록이 끝나면 RELEASED 를 true 로 바꾸세요.
   그러면 내려받기 버튼이 열리고, QR 코드가 스토어 주소로 다시 그려집니다.
   ============================================================ */

window.SITE_CONFIG = {

  /* ---- 1. 출시 상태 ------------------------------------------------
     스토어에 올라가면 false → true.
     false 인 동안에는 "곧 열립니다" 안내가 뜨고 버튼이 잠깁니다.     */
  RELEASED: false,

  /* ---- 2. 구글 플레이 ----------------------------------------------
     패키지 이름은 브리프에 정해진 값입니다. 바꾸지 마세요.           */
  PACKAGE_ID: 'kr.palpal.phone88',

  /* ---- 3. 값 --------------------------------------------------------
     앱은 무료이고, 아래 금액을 한 번 내면 광고가 사라집니다.
     구독이 아닙니다.                                                  */
  REMOVE_ADS_PRICE: '8,800원',

  /* ---- 4. 문의 받을 주소 --------------------------------------------
     ※ 스토어에 적는 주소와 같아야 합니다.
     ※ 아직 88폰 전용 주소를 받지 못해 스튜디오 주소를 씁니다.
        전용 주소가 생기면 여기만 바꾸세요.                            */
  SUPPORT_EMAIL: 'HNStudio.c@gmail.com',

  /* ---- 5. 이 페이지 주소 (QR 코드에 씁니다) -------------------------- */
  SITE_URL: 'https://www.hanullstudio.com/88phone/',

  /* ---- 6. 전자상거래 사업자 정보 -------------------------------------
     유료 결제가 있으므로 바닥글에 반드시 표기해야 하는 항목입니다.
     ※ 아직 실제 값을 받지 못했습니다. 아래를 채우면 바닥글에 나타나고,
       비어 있으면 "준비 중"으로 표시됩니다.                           */
  BUSINESS: {
    name:      '',   // 상호      예: 하늘스튜디오
    ceo:       '',   // 대표자
    regNo:     '',   // 사업자등록번호
    mailOrder: '',   // 통신판매업 신고번호 (있으면)
    address:   '',   // 주소
    phone:     ''    // 전화번호
  },

  /* ---- 7. 개인정보처리방침 시행일 ------------------------------------ */
  PRIVACY_DATE: '2026년 9월 1일',

  /* ---- 8. 후기·개선요청을 구글 폼으로 받고 싶을 때 --------------------
     주소를 넣으면 그 폼으로 연결되고, 비워 두면 메일로 보냅니다.      */
  REVIEW_FORM_URL: '',
  FEEDBACK_FORM_URL: ''
};

/* ---- 위 값에서 자동으로 만들어지는 주소 (수정 불필요) ---- */
(function (cfg) {
  cfg.PLAY_STORE_URL =
    'https://play.google.com/store/apps/details?id=' + cfg.PACKAGE_ID;
  cfg.PLAY_STORE_APP_URL = 'market://details?id=' + cfg.PACKAGE_ID;
  cfg.PLAY_STORE_REVIEW_URL = cfg.PLAY_STORE_URL + '&showAllReviews=true';
})(window.SITE_CONFIG);
